# Changelog

## 1.0 — 首次公开发布 / first public release

首个公开版本，包含此前所有内部迭代的成果。

**核心能力**

- 三种模式：批量压缩（整卡 / 文件夹）、单服装卡细分压缩（按部位）、单张人物卡压缩（按组·部位）
- 贴图类型分别设置「处理方式」与「最大边」；可一键统一，也可逐类、逐部位、逐张指定
- 预设：调好一张卡后导出，套用到同款服装的其它卡（按「部位|材质|属性」签名匹配）

**压缩与画质**

- 无损再压缩：PNG 自适应滤波 + 灰度降 2 通道、JPEG 最优哈夫曼表（像素完全不变）
- 面积平均降采样（数据贴图）+ 边缘感知锐化（对比度自适应 / USM 可选）
- 独占贴图保护：只被一个部位引用的贴图不跟着降分辨率
- 支持 Radiance HDR 环境反射贴图、8192px 贴图、Alpha 遮罩、彩色掩罩

**界面**

- 四种界面语言：中文 / English / 日本語 / 한국어（含气泡提示）
- 浅色（纯白）/ 深色主题：自绘页签、自绘滑块、扁平按钮、深色标题栏与滚动条
- 四个表格的列宽可拖动、可记住、可恢复默认；贴图可弹出独立窗口放大查看

**命令行**

- `scan` / `parts` / `preset` / `compress` / `batch`，完整参数见 `KoiCardTexTool.exe help`

**工程**

- .NET 8 + WinForms，无 NuGet 依赖，无捆绑第三方二进制
- `tools/build.ps1` 构建、`tools/selftest.ps1` 自检（26 项：结构读取、界面、语言切换、端到端压缩）
